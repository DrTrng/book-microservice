name: Build Test

on:
  push:
    branches:
      - "**"

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      # 👇 STEP DEBUG (RẤT QUAN TRỌNG)
      - name: Debug repo structure
        run: |
          pwd
          ls -la
      - name: Just build
        run: echo "CI is running"